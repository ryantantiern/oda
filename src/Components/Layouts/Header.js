import React from 'react'
import { AppBar, Toolbar, Typography } from '@material-ui/core'

export default props => 
<div>
  <AppBar position="static" color='primary'>
    <Toolbar>
      <Typography variant='h3' color='inherit'>Oda</Typography>
    </Toolbar>
  </AppBar>
</div>
